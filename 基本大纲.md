# 私有文件管理系统 — 技术方案 v1

---

## 一、背景与核心需求

### 1.1 数据规模

| 维度 | 数据 |
|---|---|
| 总存储量 | 约 140GB（未来常数级增长，不会大幅增加） |
| 文件总数 | 约 7000 个 |
| 长视频（>50MB） | 占总存储 15%，占文件数不足 1% |
| 中短视频 | 占总存储 65%，占文件数 65% |
| 图片 | 占总存储 10%，占文件数 30% |
| 文档/文本等 | 剩余部分 |

### 1.2 核心目标

1. **源文件不可见**：文件不以原始形态存在于文件系统中，普通用户无法通过资源管理器或主流程序直接打开。不要求密码学级别的加密，只需"封装/打散"即可。
2. **自建文件管理器**：提供与 Windows 资源管理器基本对等的文件管理功能。
3. **性能要求**：
   - 目录列表快速显示（含缩略图）。
   - 视频/图片打开时加载时间短，视频支持 seek。
4. **开发方式**：使用 AI IDE 快速开发，优先复用成熟开源模块。

### 1.3 约束

- 仅供个人学习使用，不分发二次使用。
- 加密不是硬性要求，数据可以不加密，只要能实现"普通人范围内不可见"即可。
- 数据库规模为常数级，不需要考虑分布式或高并发。

---

## 二、功能需求清单

### 2.1 核心功能

| 功能 | 说明 | 优先级 |
|---|---|---|
| 文件名显示 | 列表/网格视图显示文件名、大小、修改时间 | P0 |
| 缩略图显示 | 图片和视频显示缩略图，支持虚拟滚动 | P0 |
| 图片打开 | 内置查看器或调用系统默认程序 | P0 |
| 视频播放 | 临时文件 + 系统播放器，支持 seek | P0 |
| 其他格式打开 | 解出临时文件后调用第三方软件（notepad、WPS 等） | P0 |
| 搜索 | 文件名搜索 + 文档内容全文搜索 | P1 |
| 文件操作 | 复制、粘贴、剪切、新建、删除、重命名 | P0 |
| 文件夹操作 | 新建、删除、重命名、移动 | P0 |
| 导入/导出 | 文件选择对话框 + 目录选择，支持多选 | P0 |
| 拖拽交互 | 从资源管理器拖入导入，拖出导出 | P1 |
| 剪贴板交互 | 与 Windows 资源管理器双向复制粘贴 | P2 |

### 2.2 交互细节

- **双击打开**：图片→内置查看器；视频→系统默认播放器；TXT→notepad；DOCX/XLSX→WPS；其他→系统默认程序。
- **右键菜单**：打开、打开方式、复制、剪切、重命名、删除、导出到…
- **多选**：Ctrl/Shift 多选，批量操作。
- **导入**：点击导入按钮 → 弹出文件/文件夹选择框（支持多选）→ 选择导入目标目录 → 处理入库。
- **导出**：选中文件 → 点击导出 → 弹出目录选择框 → 导出到指定目录。

---

## 三、技术方案

### 3.1 整体架构

```
┌──────────────────────────────────────────────────┐
│                   Tauri v2 应用                    │
│                                                    │
│  ┌──────────────┐        ┌──────────────────┐    │
│  │  React 前端   │◄─────►│   Rust 后端       │    │
│  │ (Ant Design) │  IPC  │   (业务逻辑)      │    │
│  └──────────────┘        └────────┬─────────┘    │
│                                    │               │
│                          ┌────────▼─────────┐    │
│                          │   SQLite 数据库    │    │
│                          │    (元数据)        │    │
│                          └──────────────────┘    │
│                                    │               │
│                     ┌──────────────┼──────────┐  │
│                     │              │          │  │
│              ┌──────▼───┐  ┌──────▼───┐  ┌───▼──────┐
│              │ 二进制存储 │  │ 缩略图   │  │ 临时文件  │
│              │ 文件(分片) │  │ 缓存目录 │  │ 目录     │
│              └──────────┘  └──────────┘  └──────────┘
└──────────────────────────────────────────────────┘
```

**技术选型**：

| 模块 | 选型 | 理由 |
|---|---|---|
| 桌面框架 | Tauri v2 | Rust 后端 + WebView 前端，单二进制，插件生态覆盖文件/对话框/外部程序调用 |
| 前端框架 | React + TypeScript | 生态成熟，AI 辅助开发效率高 |
| UI 组件库 | Ant Design | 组件齐全（Tree、Table、Modal、Menu 等），中文文档完善 |
| 数据库 | SQLite（rusqlite） | 仅存元数据，零配置，常数级规模完全够用 |
| 二进制数据存储 | 独立分片文件（多文件方案） | SQLite 保持轻量，二进制数据存独立文件，按 2GB 分片 |
| 缩略图生成 | image crate + ffmpeg 命令行 | 入库时后台队列异步执行，限制并发数 |
| 缩略图缓存 | 独立文件 + SQLite 索引表 | 利用文件系统 page cache |
| 搜索 | SQLite LIKE + FTS5（trigram） | 文件名 + 文档内容 |
| 外部程序调用 | tauri-plugin-opener + Command::new | 临时文件 + 指定应用 |
| 导入/导出 | tauri-plugin-dialog | 文件/文件夹选择对话框 |

### 3.2 存储层设计

#### 核心思路：元数据与二进制数据分离

SQLite 仅存储元数据和 chunk 位置索引（偏移量、长度），实际二进制数据存储在独立的分片文件中。每个分片文件最大 2GB，按顺序追加写入。

**优势**：
- SQLite 库保持几十 MB 级别，备份秒级完成
- 分片文件是纯二进制，无扩展名，普通人无法直接打开
- 按 2GB 分文件，避免单文件过大
- 读取时根据 `store_file + offset + length` 做 seek + read，性能优秀

#### 目录结构

```
data/
  metadata.db          -- SQLite 元数据数据库
  store/
    store_001.bin      -- 二进制分片文件（最大 2GB）
    store_002.bin
    store_003.bin
    ...
  thumb_cache/
    img/{file_id}.webp -- 图片缩略图
    vid/{file_id}.webp -- 视频缩略图
  temp/                -- 临时文件目录（打开文件用）
```

#### 表结构

```sql
-- 文件元数据
CREATE TABLE files (
  file_id       INTEGER PRIMARY KEY,
  name          TEXT NOT NULL,
  ext           TEXT,
  mime_type     TEXT,
  size_bytes    INTEGER,
  duration_sec  REAL,
  width         INTEGER,
  height        INTEGER,
  category      TEXT,            -- 'video_long','video_short','image','doc'
  created_at    INTEGER,
  updated_at    INTEGER
);

-- Chunk 位置索引（指向分片文件中的偏移和长度）
CREATE TABLE chunk_locations (
  file_id     INTEGER NOT NULL,
  chunk_index INTEGER NOT NULL,
  store_file  TEXT NOT NULL,     -- 分片文件名，如 'store_001.bin'
  offset      INTEGER NOT NULL,  -- 在分片文件内的起始偏移
  length      INTEGER NOT NULL,  -- 字节数
  PRIMARY KEY (file_id, chunk_index)
);

-- 分片文件使用记录
CREATE TABLE store_files (
  store_file   TEXT PRIMARY KEY,
  used_bytes   INTEGER DEFAULT 0, -- 已使用字节数
  created_at   INTEGER
);

-- 虚拟文件夹
CREATE TABLE folders (
  folder_id  INTEGER PRIMARY KEY,
  name       TEXT NOT NULL,
  parent_id  INTEGER,
  created_at INTEGER
);

-- 文件-文件夹关联
CREATE TABLE file_folder (
  file_id   INTEGER NOT NULL,
  folder_id INTEGER NOT NULL,
  PRIMARY KEY (file_id, folder_id)
);

-- 缩略图索引
CREATE TABLE thumbnail_index (
  file_id      INTEGER PRIMARY KEY,
  thumb_path   TEXT,
  generated_at INTEGER
);

-- 全文搜索（FTS5）
CREATE VIRTUAL TABLE file_search USING fts5(
  file_id UNINDEXED,
  name,
  content,
  tokenize='trigram'
);
```

#### 分块策略

| 文件类型 | 块大小 | 说明 |
|---|---|---|
| 文档/小图片（<100KB） | 整文件单 chunk | 直接写入一个分片文件 |
| 中等文件（100KB–50MB） | 64KB | 分多个 chunk 写入 |
| 大视频（>50MB） | 256KB | 分多个 chunk 写入 |

**写入流程**：
1. 读取源文件，按块大小切分。
2. 选择当前未满的分片文件（`used_bytes < 2GB`），若无则创建新分片。
3. 将每个 chunk 追加写入分片文件尾部，记录 `store_file + offset + length` 到 `chunk_locations`。
4. 更新 `store_files.used_bytes`。

**读取流程**：
1. 查询 `chunk_locations` 获取该文件所有 chunk 的位置。
2. 按 `chunk_index` 顺序，打开对应分片文件，seek 到 offset，读取 length 字节。
3. 拼接写入临时文件。

### 3.3 缩略图策略

- **生成时机**：入库时后台队列异步生成，不阻塞入库流程。
- **并发控制**：最多 2 个 ffmpeg 进程同时运行，避免占满 CPU。
- **图片**：`image` crate 读取 → 缩放至宽 256px → 输出 WebP。
- **视频**：先从分片文件读取头部 1MB 到临时文件 → `ffmpeg -i <临时文件> -ss 10% -vframes 1 -vf scale=256:-1 thumb.webp`。
- **缓存目录**：`thumb_cache/img/{file_id}.webp` 和 `thumb_cache/vid/{file_id}.webp`。
- **列表页查询**：一次 JOIN 查询拿到当前页所有缩略图路径，前端并行加载。
- **内存 LRU**：维护 200 张缩略图的内存缓存。
- **生成中状态**：前端显示占位图，生成完成后自动刷新。

### 3.4 视频播放

- **方案**：临时文件方案。双击视频时，从分片文件读取所有 chunk → 拼接写入临时文件 → 调用系统默认播放器打开。
- **性能**：NVMe SSD 上写入 100MB 约 40-100ms，加上播放器启动约 1.5-2 秒出画面；50MB 视频约 1-1.5 秒。
- **MP4 faststart**：入库时对 MP4 视频用 `ffmpeg -movflags faststart` 预处理，把 moov atom 移到文件头部，加快播放器加载。
- **清理**：播放器关闭后清理临时文件，或定时清理超过 24 小时的文件。

### 3.5 文件操作

| 操作 | 实现 |
|---|---|
| 重命名 | `UPDATE files SET name = ? WHERE file_id = ?` |
| 删除 | 事务内删除 chunk_locations + thumbnail_index + file_folder + files，分片空间标记回收 |
| 复制 | 复制 files 行 + 读取源 chunk 写入新分片 + 写入新 chunk_locations |
| 移动 | `UPDATE file_folder SET folder_id = ? WHERE file_id = ?` |
| 新建文件夹 | `INSERT INTO folders (name, parent_id) VALUES (?, ?)` |

所有操作在 SQLite 事务内完成，保证原子性。

### 3.6 导入/导出

**导入**：
1. 用户点击导入按钮 → 弹出文件/文件夹选择对话框（支持多选）。
2. 选择导入目标文件夹（可选，默认当前文件夹）。
3. 逐个执行入库流程：读取源文件 → 分块写入分片文件 → 插入元数据 → 后台生成缩略图 → 建立搜索索引。
4. 可选删除源文件。
5. 显示导入进度。

**导出**：
1. 用户选中文件 → 点击导出按钮 → 弹出目录选择对话框。
2. 从分片文件读取 chunk → 拼接写入目标目录，恢复原始文件名。
3. 支持批量导出。

### 3.7 第三方软件调用

**流程**：双击文件 → 从分片文件读取 chunk → 写入临时文件 → 调用目标程序 → 程序关闭后清理。

**调用方式**：

| 文件类型 | 调用目标 |
|---|---|
| TXT | `notepad.exe <临时文件>` |
| DOCX/XLSX | `wps.exe <临时文件>`（从注册表读 WPS 路径） |
| 图片 | 内置查看器 |
| 视频 | 系统默认播放器 |
| 其他 | `tauri-plugin-opener` 系统默认程序 |

**临时文件管理**：专用目录 `data/temp/`，随机文件名，退出时清空，定时清理超过 24 小时的文件。

### 3.8 搜索

- **文件名搜索**：`SELECT * FROM files WHERE name LIKE '%关键词%' LIMIT 50`。
- **全文搜索**：FTS5 + trigram 分词器，入库时对可提取文本的文件（TXT、MD、代码）提取内容写入 `file_search`。
- **查询**：`SELECT f.* FROM file_search s JOIN files f ON f.file_id = s.file_id WHERE file_search MATCH ? ORDER BY rank LIMIT 50`。

### 3.9 UI 层

**方案**：基于 Tauri v2 + React + Ant Design 从头搭建。

**核心组件**：
- **目录树**：Ant Design `Tree` 组件，展示文件夹层级。
- **文件列表/网格**：列表视图用 `Table`，网格视图自定义 + 缩略图。
- **虚拟滚动**：`@tanstack/react-virtual`，只渲染可视区域内的缩略图。
- **右键菜单**：Ant Design `Dropdown` + `Menu`。
- **面包屑导航**：Ant Design `Breadcrumb`。
- **搜索框**：Ant Design `Input.Search`。
- **导入/导出**：Ant Design `Button` + tauri-plugin-dialog 文件选择。

**布局**：
```
┌─────────────────────────────────────────────┐
│  面包屑导航 / 搜索框 / 工具栏               │
├──────────┬──────────────────────────────────┤
│          │                                  │
│  目录树   │    文件列表 / 网格视图            │
│          │    （含缩略图、虚拟滚动）           │
│          │                                  │
│          │                                  │
├──────────┴──────────────────────────────────┤
│  状态栏（文件数、总大小、当前路径）            │
└─────────────────────────────────────────────┘
```

---

## 四、开发路线图

### 阶段一：存储骨架
1. 初始化 Tauri v2 + React + Ant Design 项目。
2. 建 SQLite 表结构 + 分片文件管理模块。
3. 实现入库函数：读取源文件 → 分块写入分片文件 → 记录 chunk_locations → 提取元数据 → 插入 files。
4. 实现读取函数：根据 chunk_locations 从分片文件读取 → 拼接写入临时文件。
5. 用 10 个文件（含视频）测试分块读写和临时文件打开。

### 阶段二：文件管理器 UI
6. 搭建主界面布局：目录树 + 文件列表/网格 + 工具栏。
7. 实现目录树导航 + 文件列表查询。
8. 接入缩略图显示和虚拟滚动。
9. 实现缩略图后台生成队列（限制并发）。

### 阶段三：文件操作与打开
10. 实现重命名、删除、复制、移动（SQLite 事务）。
11. 实现双击打开（临时文件 + 系统程序调用）。
12. 实现右键菜单。

### 阶段四：导入/导出
13. 实现批量导入（文件选择对话框 + 入库 + 缩略图生成 + 进度显示）。
14. 实现批量导出（从分片文件读取 → 写入目标目录）。

### 阶段五：搜索与增强
15. 接入 FTS5 全文搜索。
16. 拖拽导入/导出。
17. 剪贴板交互（与资源管理器双向复制粘贴）。

---

## 五、关键风险与注意事项

| 风险 | 应对 |
|---|---|
| 分片文件空间回收 | 删除文件后标记空间为可用，或定期合并压缩分片文件 |
| 7000 个文件同时渲染卡顿 | 虚拟滚动 + 缩略图 LRU 缓存 |
| 临时文件暴露原始内容 | 随机文件名 + 退出清理 + 短生命周期 |
| 数据库文件损坏 | 定期备份 + SQLite WAL 模式 + 事务保证原子性 |
| 入库大量文件时 CPU 占满 | 缩略图生成限制并发（最多 2 个 ffmpeg），入库队列串行处理 |
| 分片文件写入冲突 | 单线程写入，写入时加锁 |

---

## 六、开发分工

**AI 负责**：
- 项目初始化和整体架构搭建。
- SQLite 表结构创建和 CRUD 函数。
- 分片文件读写 Rust 代码。
- 缩略图生成的 ffmpeg/image crate 调用。
- React 前端组件（目录树、文件列表、网格、右键菜单、搜索框）。
- 虚拟滚动集成。
- FTS5 搜索查询。
- 临时文件管理和第三方程序调用。
- 导入/导出功能。

**人工辅助**：
- 环境配置和依赖安装。
- 运行测试和反馈问题。
- 方案选择和关键决策。
- 错误调试和边界情况处理。
